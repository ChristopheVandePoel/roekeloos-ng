import { WatchScrollDirective } from './watch-scroll.directive';

describe('WatchScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new WatchScrollDirective();
    expect(directive).toBeTruthy();
  });
});
